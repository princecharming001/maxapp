Done. F-009 closed. Root cause was `_BLOCK_TYPE_PATTERNS` matching the verb "compare", causing "compare X vs Y in a table" to trigger the multi-block path (count=2) instead of the single-table explicit path. Removed "compare" from the pattern — only "comparison" noun and "pros and cons" now match the comparison slot. VIS-07 passes seeds 6, 9, 29; no neighboring regressions; 778 tests pass.

Next iteration should fix F-021 (VIS-12 multi-block: only 2/4 block types emitted) or F-022 (VIS-03 prose_nonempty=0 for stat_cards-only response).
