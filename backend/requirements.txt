# FastAPI and Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
pgvector>=0.3.0
greenlet>=3.0.0
python-docx>=1.1.0
pdfplumber>=0.11.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.2

# Validation
pydantic>=2.5.3
pydantic-settings>=2.1.0
email-validator>=2.1.0
pyyaml>=6.0

# Stripe Payments
stripe>=7.12.0

# Google Gemini (standalone - minimal dependencies)
google-generativeai>=0.3.2

# Anthropic Claude
anthropic>=0.40.0
langchain-anthropic>=0.3.0

# OpenAI (when LLM_PROVIDER=openai)
openai>=1.40.0

# Mistral (when LLM_PROVIDER=mistral)
mistralai>=1.0.0

# LangChain — agentic framework for LLM/chat + RAG embeddings
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-openai>=0.2.0,<0.3.0
langchain-google-genai>=2.0.10,<2.1.0
langchain-mistralai>=0.2.0,<0.3.0
langchain-community>=0.3.0,<0.4.0

# RAG extras — tiktoken for chunk sizing + token budgeting
tiktoken>=0.7.0

# LangGraph — state-machine orchestration for chat
langgraph>=0.2.0,<0.3.0

# Cloud Storage
boto3>=1.34.25

# Utilities
python-dotenv>=1.0.0
httpx[http2]>=0.26.0
PyJWT[crypto]>=2.8.0
Pillow>=11.0.0

# Background Scheduler
apscheduler>=3.10.0

# Web-search failsafe (fast_rag tier 2.5 — fetched when both targeted +
# broad doc retrieval miss). DuckDuckGo via ddgs: free, no API key.
ddgs>=8.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.3
